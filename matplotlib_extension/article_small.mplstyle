## combined theme: article.mplstyle + small.mplstyle
## use as plt.style.use('matplotlib_extension.article_small')
## equivalent to plt.style.use(['matplotlib_extension.article', 'matplotlib_extension.small'])
##
## NOTE: this file merges article.mplstyle and small.mplstyle (small's values win on
## keys defined in both, matching that usage order). If either of those files changes,
## update this file to match — tests/test_style.py asserts the two stay equivalent.

#### MATPLOTLIBRC FORMAT

## Matplotlib configuration are currently divided into following parts:
##     - BACKENDS
##     - LINES
##     - PATCHES
##     - HATCHES
##     - BOXPLOT
##     - FONT
##     - TEXT
##     - LaTeX
##     - AXES
##     - DATES
##     - TICKS
##     - GRIDS
##     - LEGEND
##     - FIGURE
##     - IMAGES
##     - CONTOUR PLOTS
##     - ERRORBAR PLOTS
##     - HISTOGRAM PLOTS
##     - SCATTER PLOTS
##     - AGG RENDERING
##     - PATHS
##     - SAVING FIGURES
##     - INTERACTIVE KEYMAPS
##     - ANIMATION

## https://github.com/matplotlib/matplotlib/blob/main/lib/matplotlib/mpl-data/matplotlibrc

mathtext.fontset: custom # dejavusans  # Should be 'dejavusans' (default),
                               # 'dejavuserif', 'cm' (Computer Modern), 'stix',
                               # 'stixsans' or 'custom'
mathtext.bf:  Times New Roman:bold # sans:bold
mathtext.bfit: Times New Roman:italic:bold # sans:italic:bold
mathtext.it:  Times New Roman:italic # sans:italic
mathtext.rm:  Times New Roman # sans
mathtext.fallback: cm  # Select fallback font from ['cm' (Computer Modern), 'stix'
                        # 'stixsans'] when a symbol cannot be found in one of the
                        # custom math fonts. Select 'None' to not perform fallback
                        # and replace the missing character by a dummy symbol.
mathtext.default: it  # The default font to use for math.
                       # Can be any of the LaTeX font names, including
                       # the special name "regular" for the same font
                       # used in regular text.

font.size:    15 # 10.0  # from small.mplstyle

axes.labelcolor:    black

xtick.top:           True # False   # draw ticks on the top side
xtick.bottom:        True    # draw ticks on the bottom side
xtick.direction:     in # out     # direction: {in, out, inout}
xtick.labelsize:     12.5 # medium  # from small.mplstyle

ytick.left:          True    # draw ticks on the left side
ytick.right:         True # False   # draw ticks on the right side
ytick.direction:     in # out     # direction: {in, out, inout}
ytick.labelsize:     12.5 # medium  # from small.mplstyle


legend.borderpad:     0.2 # 0.4  # border whitespace
legend.labelspacing:  0.1 # 0.5  # the vertical space between the legend entries
legend.borderaxespad: 0.4 # 0.5  # the border between the axes and legend edge
legend.fontsize:      15 # medium  # from small.mplstyle
legend.title_fontsize: 15 # None    # from small.mplstyle

figure.figsize:     3.2, 2.4 # 6.4, 4.8  # figure size in inches
figure.dpi:         200 # 100       # figure dots per inch
figure.facecolor:   none # white     # figure face color

figure.subplot.left:   0.27 # 0.125, 0.25 in article  # from small.mplstyle
figure.subplot.right:  0.93 # 0.9, 0.95 in article    # from small.mplstyle
figure.subplot.bottom: 0.27 # 0.11, 0.2 in article   # from small.mplstyle
figure.subplot.top:    0.93 # 0.88 in article         # from small.mplstyle

errorbar.capsize: 3.5 # 0  # length of end cap on error bars in pixels

savefig.transparent: False     # whether figures are saved with a transparent
                                # background by default
